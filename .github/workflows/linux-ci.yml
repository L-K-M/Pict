name: Linux CI

on:
  pull_request:
  push:
    branches: [main]
  # Manual runs, matching ci.yml: dispatch goes through the API, so it still
  # starts a run when webhook delivery is throttled, and re-running CI needs no
  # empty commit.
  workflow_dispatch:

# This job only checks out code and runs the build and tests — it never needs a
# write-scoped token. Pin it to read so a buggy or hostile build/test step run
# inside the container can't reach anything else with the default token.
permissions:
  contents: read

# Cancel an in-progress run when a branch is pushed again.
concurrency:
  group: linux-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build & Test PictKit (Linux)
    runs-on: ubuntu-latest
    # A clean build + test is a couple of minutes; anything approaching this is a
    # hang. Fail fast instead of burning the 360-minute default.
    timeout-minutes: 30
    # swift:6.3-noble == swift:6.3 (Ubuntu 24.04); the toolchain that ships the
    # Linux Foundation these files are ported against.
    container: swift:6.3-noble
    steps:
      - uses: actions/checkout@v4

      # SwiftPM shells out to git to resolve packages; inside the container the
      # checkout is owned by a different uid than the one running the build, which
      # git refuses to touch until the workspace is marked safe.
      - name: Mark the workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Build
        run: swift build -v

      - name: Test
        run: swift test -v
